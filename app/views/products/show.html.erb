<div class="flex flex-col gap-4">
  <h1 class="text-xl"><%= @product.name %></h1>
  <%= link_to "Back", products_path, class: "text-blue-500" %>
  <%= link_to "Edit", edit_product_path(@product), class: "text-blue-500" %>
</div>